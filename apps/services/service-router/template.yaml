AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  service-router
  
  Lambda function that routes chat messages to AWS Bedrock Agent

Globals:
  Function:
    Timeout: 30
    Runtime: nodejs20.x
    Architectures:
      - x86_64
    LoggingConfig:
      LogFormat: JSON

Parameters:
  Environment:
    Type: String
    Default: prod
    AllowedValues:
      - prod
    Description: Environment name
  
  BedrockAgentId:
    Type: String
    Description: AWS Bedrock Agent ID
    NoEcho: true
  
  BedrockAgentAliasId:
    Type: String
    Default: TSTALIASID
    Description: AWS Bedrock Agent Alias ID
  
  ChatHistoryTableName:
    Type: String
    Description: DynamoDB table name for chat history
  
  LogLevel:
    Type: String
    Default: info
    AllowedValues:
      - error
      - warn
      - info
      - debug
    Description: Logging level

Resources:
  ServiceRouterFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ./
      Handler: app.lambdaHandler
      Description: Routes chat messages to AWS Bedrock Agent
      MemorySize: 512
      Environment:
        Variables:
          ENVIRONMENT: !Ref Environment
          BEDROCK_AGENT_ID: !Ref BedrockAgentId
          BEDROCK_AGENT_ALIAS_ID: !Ref BedrockAgentAliasId
          CHAT_HISTORY_TABLE_NAME: !Ref ChatHistoryTableName
          LOG_LEVEL: !Ref LogLevel
          AWS_NODEJS_CONNECTION_REUSE_ENABLED: '1'
      Policies:
        - Statement:
            - Sid: InvokeBedrock
              Effect: Allow
              Action:
                - bedrock:InvokeAgent
                - bedrock:InvokeModel
              Resource: '*'
        - DynamoDBCrudPolicy:
            TableName: !Ref ChatHistoryTableName
      Events:
        ChatApi:
          Type: Api
          Properties:
            Path: /chat
            Method: post
            RestApiId: !Ref ServiceRouterApi
        OptionsApi:
          Type: Api
          Properties:
            Path: /chat
            Method: options
            RestApiId: !Ref ServiceRouterApi

  ServiceRouterApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      Cors:
        AllowOrigin: "'*'"
        AllowHeaders: "'Content-Type,Authorization'"
        AllowMethods: "'GET,POST,OPTIONS'"
        MaxAge: "'86400'"

Outputs:
  ServiceRouterApi:
    Description: API Gateway endpoint URL for Service Router
    Value: !Sub "https://${ServiceRouterApi}.execute-api.${AWS::Region}.${AWS::URLSuffix}/Prod/chat"
  
  ServiceRouterFunction:
    Description: Service Router Lambda Function ARN
    Value: !GetAtt ServiceRouterFunction.Arn
  
  ServiceRouterFunctionIamRole:
    Description: Implicit IAM Role created for Service Router function
    Value: !GetAtt ServiceRouterFunctionRole.Arn

